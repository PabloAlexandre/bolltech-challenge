import { CreateProject } from "./components/create-project";

export const Dashboard = () => {
  return (
    <main className="dashboard">
      <section>

      </section>
      <CreateProject />
    </main>
  );
}